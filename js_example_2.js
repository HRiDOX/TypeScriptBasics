const user = {
    firstName : "Hridoy",
    lastName  : "Hridoy",
    role      : "ADMIN",
 };

 // SUPPOSE tHIS OBJECT come from server

 //or we import this


 console.log(user);

// suppose we don't know what should we call after user. as it is comming from server we need to simply run user. to what object actually it has

// { firstName: 'Hridoy', lastName: 'Hridoy', role: 'ADMIN' } we'll found this output yeah we have this